import 'package:flutter/material.dart';

// Define el color del AppBar en la aplicación.
const Color appBarColor = Color(0xFF512DA8); // Un tono oscuro de púrpura

// Define el color del texto de los títulos en la aplicación.
const Color titleTextColor = Colors.white;

// Define el color de fondo para las pantallas.
const Color backgroundColor = Color(0xFFEDE7F6); // Un tono suave y claro de púrpura
